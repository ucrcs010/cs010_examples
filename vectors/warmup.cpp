#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{
    string word;
    
    // 1. write code to access first character of string variable word:
    
    // 2. write code to "traverse" (i.e. loop through) string
    //    and output each character of word to screen, space separated
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    // 3. Now traverse vector<string> words, and output to screen
    //    each word on a line by itself
    
    
    // 4. Now traverse vector<string> words, and output to screen
    //    FIRST CHARACTER of each word on a line by itself
    
    
    // 5. Now traverse vector<string> words, and output to screen
    //    each character of each word (space separated),
    //    each word on a line by itself.
    
    
    return 0;
}